# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=infinicard
DB_USER=infinicard_user
DB_PASSWORD=infinicard_pass_2024

# API Configuration
API_PORT=3000
API_HOST=0.0.0.0
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=*

# File Upload Configuration
MAX_FILE_SIZE=10485760
UPLOAD_DIR=./uploads

# OCR Configuration (if using backend OCR)
OCR_ENGINE=tesseract
TESSERACT_PATH=/usr/bin/tesseract

# Logging
LOG_LEVEL=debug

# Mail / SMTP (development)
# Defaults will use MailHog when running locally via docker-compose
SMTP_HOST=127.0.0.1
SMTP_PORT=1025
SMTP_USER=jahnviaghera@gmail.com
SMTP_PASS=yjrreucvzevwwmrs
SMTP_SECURE=false
MAIL_FROM=no-reply@infinicard.app

# Force all outgoing emails to this address (useful for testing). Leave empty to disable.
# Example: FORCE_SEND_TO=jahnviaghera@gmail.com
FORCE_SEND_TO=jahnviaghera@gmail.com

# If you want to send real emails via Gmail (example):
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your.email@gmail.com
# SMTP_PASS=<app-password>
# SMTP_SECURE=false
